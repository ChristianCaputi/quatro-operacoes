let numUm;
let numDois;
let resultado;

numUm = Number(Prompt("Por favor, insira o primeiro número da subtração"));
numDois = Number(Prompt("Por fim, insira o segundo número da subtração"));
result = numUm - numDois;

    alert(`O resultado da subtração dos dois números desejados é: ${resultado.tofixed(2)}!`);